/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The type of failure - PROMPT_MODERATION_BLOCKED indicates the prompt was blocked by content moderation
 */
export enum Type {
  PromptModerationBlocked = "PROMPT_MODERATION_BLOCKED",
}

/**
 * Details about a generation failure due to prompt moderation
 */
export type PromptModerationFailureReason = {
  /**
   * Number of outputs affected by this failure
   */
  affectedOutputCount: number;
  /**
   * Human-readable message describing the failure
   */
  message: string;
  /**
   * The type of failure - PROMPT_MODERATION_BLOCKED indicates the prompt was blocked by content moderation
   */
  type: Type;
};

/** @internal */
export const Type$inboundSchema: z.ZodNativeEnum<typeof Type> = z.nativeEnum(
  Type,
);

/** @internal */
export const PromptModerationFailureReason$inboundSchema: z.ZodType<
  PromptModerationFailureReason,
  z.ZodTypeDef,
  unknown
> = z.object({
  affectedOutputCount: z.number().int(),
  message: z.string(),
  type: Type$inboundSchema,
});

export function promptModerationFailureReasonFromJSON(
  jsonString: string,
): SafeParseResult<PromptModerationFailureReason, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PromptModerationFailureReason$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PromptModerationFailureReason' from JSON`,
  );
}
